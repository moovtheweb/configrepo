server:
  port: ${PORT:8100}

name: ' Marvel World '

security:
  basic:
    enabled: false
management:
  security:
    enabled: false

endpoints:
  health:
    sensitive: false
  shutdown:
    enabled: true
